[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.build]
cmds = ["dotnet restore", "dotnet build --configuration Release --no-restore"]

[start]
cmd = "dotnet run --no-build --configuration Release --urls http://0.0.0.0:$PORT"